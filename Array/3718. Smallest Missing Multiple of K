class Solution {
    public int missingMultiple(int[] nums, int k) {
        HashSet<Integer> st=new HashSet<>();
        for(int num:nums) st.add(num);
        int smallest=k;
        while(st.contains(smallest)) smallest+=k;
        return smallest;
    }
}
