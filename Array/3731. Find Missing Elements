class Solution {
    public List<Integer> findMissingElements(int[] nums) {
        List<Integer> res=new ArrayList<>();
        Arrays.sort(nums);
        int curr=nums[0],i=0;
        while(i<nums.length){
            if(curr<nums[i]) res.add(curr);
            else i++;
            curr++;
        }
        return res;
    }
}
