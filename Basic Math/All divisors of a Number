class Solution {
    public static void print_divisors(int n) {
        // code here
        ArrayList<Integer> ans=new ArrayList<>();
        for(int i=1;i*i<=n;i++){
            if(n%i==0){
                if(n/i==i) ans.add(i);
                else{
                  ans.add(i);
                  ans.add(n/i);
                }
            }
        }
        Collections.sort(ans);
        for(int x:ans)
            System.out.print(x+" ");
    }
}
